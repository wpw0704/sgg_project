#include <stdio.h>
int main()
{
  // 流程控制:程序的代码执行的过程顺序
  // 1. 顺序控制语句
    // 代码从上到下的一个执行顺序,顺序语句
  // 2. 分支控制语句
    // if语句,if-else语句,if-else_if-else_if语句,if-else_if-else_if-else语句,switch-case语句,三元表达式
  // 3. 循环控制语句
    // while循环,do-while循环,for循环
  return 0;
}