#include <stdio.h>
int main()
{
  // 数组长度计算:

  // int nums[] = {10, 20, 30, 40, 50};
  // int nums_length = sizeof(nums); // 计算数组的总字节数
  // // int data_length = sizeof(int); // 计算当前数组中元素类型的字节数
  // printf("%zu\n", nums_length);   // 20个
  // // printf("%zu\n", data_length);   //  4个

  // int nums[] = {10, 20, 30, 40, 50};
  // int nums_length = sizeof(nums); // 计算数组的总字节数
  // int data_length = sizeof(nums[0]); // 计算数组中第一个元素的字节数
  // printf("%zu\n", nums_length);   // 20个
  // printf("%zu\n", data_length);   // 4个
  // int length = nums_length / data_length;
  // printf("数组长度为:%d\n",length);

  return 0;
}